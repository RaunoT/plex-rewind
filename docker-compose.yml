services:
  plex-rewind:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - 8383:8383
    volumes:
      - ./src/config/settings.json:/app/src/config/settings.json
